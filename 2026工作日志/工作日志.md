# 2026 工作日志

## 2026-09-25：src1.6 约束收益决策

- 版本：src1.6。以原仓库 src1.5 为基线建立独立目录，不修改旧版本。
- 更改内容：用终态基础分门槛比较守约与破约束任务，并在 25 ms 搜索上限内试算相关任务组和目标恢复；只在完整净收益为正且期限允许时破约束。对不超过 24 个任务的 Stage 2 题，核算未知目标可能收益、在初始已完成目标存在争议时感知当前位置，并利用阴性感知证据；更大题保留旧路径。
- 出题与验证：新增原始对照、高分和变形压力题库及可复跑脚本。以原仓库主分支 `src1.5` 为基线重编译后，高分 30 题 IT/NT 60/60 通过、28 组提高；变形 40 题 IT/NT 80/80 通过、60 组提高，均无基础分下降；历史可有效评分的 33 题中 27 题基础分提高、6 题持平。五项本地测试全部通过。测试判定器同时修正了“负收益题不得有独立有利动作”和“官方分不能超过 1000”两项错误假设。
- 范围：以上为固定种子和所列题目的验证结果，不是全局最优或普遍零误判证明。说明见 `src1.6/docs/RELEASE.md` 和 `test-results/src1.6-acceptance-20260925/REPORT.md`。

## 2026-09-24：liuyifan1.0 六道原型题与官方平台验收

- 新版本题库：`题目/liuyifan1.0`，共6道完整XML（Stage 1四道、Stage 2两道），覆盖自由终态汇聚、搬运与goto共享终态、must-near纠错、容器纠错与合法NT扰动、牺牲约束和保留约束的收益对照。未扩展为30题。
- 新增独立出题、静态检查、官方测试、报告与证据审计工具，全部放在该题库版本目录；求解器`src1.3.3-fixed`源码未修改，前后哈希核对通过。
- 运行环境：WSL2 `Ubuntu-18.04`、Ubuntu 18.04.2、g++ 7.5.0、官方`/home/yifan/env-release-2026`、5000ms。题目按2025规则设计，评分实际使用现有2026 SDK。
- 验证：6组IT/NT解析绑定与纠错后快照等价；20次原版求解器官方运行（12次自然运行、8次Stage 2固定种子复测）加6次作者参考动作官方运行，全部获得有效ASP评分，0硬超时、0客户端崩溃。主测试IT与NT各1912分，合计3824分。
- 发现：04在确认已关闭cupboard后停错终点，丢失goto microwave；其候选边际收益为正，但goto误用目标物体keep风险而eligible=false。05执行选择正确，但内部终态约束估分把曾破坏的must-closed恢复计分，比官方基础分高估20。03初始near证据平票不传播，实际经一次Sense恢复信息。
- 题目验收修订：06初稿的inside并不被官方自动解释为near，已把第三约束改为must-not-near human，重新通过参考动作验收。初稿证据与最终证据分目录保留，正式统计仅用`test-results/liuyifan1.0-20260924-final`。
- 交付：题库README、DESIGN.md、SCORE_REPORT.md、STRATEGY_REPORT.md、scores.csv；所有原始server/client日志、官方ASP终态、构建与输入哈希、参考动作均可追溯。

## 2026-09-23：1.3.3-fixed 合法题零回归审计与修复

- 基线：从工作区 `src1.3.3B` 完整复制到独立的 `src1.3.3-fixed`，原有 1.3.2、1.3.3A/B 目录未改。B 的容错（有界解析、局部非法隔离、去重 key、Preflight、malformed recovery、对象索引保护）保留。
- 修复：默认保留所有 task/constraint 出现次数、顺序与风险/计分权重；仅显式 `-deduplicate 1` 才删除重复副本。原 19 道带重复目标的比赛题不再被误删。NL give 的合法 human Y 绑定不再误拒绝。支持 S-expression 左括号后空白；简单指令仅缺最外层右括号时在下一 marker 局部恢复，不提升损坏 constraint 的内部 task。human 布局、Stage 1 大物体同位置及无关对象缺类型从全题硬拒绝改为 warning，真正不安全的机器人身份、对象注册与越界仍由 Preflight 拦截。减少成功推断/保留副本的逐项日志，降低 5 秒预算内的额外开销。
- 运行环境：WSL2 Ubuntu 18.04.2、g++ 7.5.0、官方 `/home/yifan/env-release-2026`、Stage 2 IT、5 秒时限。按用户后续要求不再测试 02；其余 35 原题分别运行 1.3.2 和 fixed 的无固定种子及固定种子对照。最终编译产物、输入和 SDK 哈希核对通过。
- 验证：33 道能原样有效评分的题目，固定同一平台随机种子后 fixed **零降分**，其中 31 道动作序列完全一致；28/29 各多完成 2 项目标、官方分各 +60。无固定种子单轮 15 题下降 32 分，首分叉为同一 AskLoc(11) 收到不同随机答复，固定种子复核行为与分数完全一致。IT/NL 共 70 对解析/WorldState/任务/约束/纠错快照，仅损坏的 04 IT 出现预期差异。四组单元测试和四组 ASan/UBSan 均通过；20 道标记非法题以显式唯一目标契约运行，保留去重/恢复验证。
- 原题 04/05 指令括号损坏使官方评分器不能生成 `vtask.lp`，原样 0 分不作为有效验收。只在测试副本补齐右括号（04 加 5 个、05 加 1 个），其余 token 不变；两版在两个副本的官方分均为 205、动作各 22 且相同。原 XML 和原样失败日志保留。
- 审计范围、逐题得分分解与严格验收边界见 `src1.3.3-fixed/AUDIT.md`、`src1.3.3-fixed/VALIDATION.md`，原始日志见 `test-results/realcompetition-src133-fixed-final-*`、`realcompetition-src133-fixed-repaired-*`。鉴于 04/05 原题评分器失败，不能宣称“原样 35 题均取得有效官方分”；可评分原题与最小修复副本的同证据实验未发现容错导致的合法题失分或策略退化。

## 2026-09-22：Ubuntu 18.04 / 26env 36 题重新测试

- 确认此前 36 题使用的是默认 WSL（g++ 15.2），不是指定环境；重新在 WSL2
  `Ubuntu-18.04`、SDK `/home/yifan/env-release-2026`、g++ 7.5.0、glibc 2.27
  中编译 `src1.3.2` 和 `src1.3.3A`，串行运行全部 36 题。
- 1.3.2 正常完成 34 题，02 被平台拒绝，08 在 5.000 s 硬超时；1.3.3A
  正常完成 35 题，仅 02 被平台拒绝，0 硬超时。
- 单轮总分 `17800 -> 18171`；两版共同成功的 34 题为
  `17318 -> 17585`。其中 24 题动作序列逐项一致；全部 6 个降分题均收到不同
  的随机询问回答。相同证据下唯一动作差异为 01，1.3.2 的动作流是 1.3.3A
  的严格前缀。
- 该环境下未发现正常题 Candidate 排序或执行策略回归。正式报告见
  `test-results/realcompetition-src132-vs-src133A-all36-ubuntu1804-26env-20260922/report.md`。

## 2026-09-22：src1.3.3A realcompetiton 36 题全量对比

- 在同一 `NFSQ4.2` SDK 下重新编译 `src1.3.2` 与 `src1.3.3A`，以 Stage 2、
  IT、5000 ms 参数串行运行 `realcompetiton_2024` 的 01–36 全部题目。
- 1.3.2 正常完成 34 题：02 被平台拒绝，03 在完成任务后的 `Fini` 阶段发生
  堆损坏并退出；1.3.3A 正常完成 35 题，仅 02 的非法 XML 无法由平台建立。
- 将未评分题按 0 计，单轮总分 `19390 -> 20120`；两版共同成功的 34 题为
  `19390 -> 19694`。20 个有效配对动作序列逐项一致；10 题差异来自不同的
  `AskLoc` / `Sense` 随机回答，01/34/36 来自既有 Deadline gate 的墙钟余量。
- `candidate_plan.cpp/.hpp` 两版哈希一致；未发现正常题 Candidate 排序或执行
  策略退化。详细结果见
  `test-results/realcompetition-src132-vs-src133A-all36-20260922/report.md`。

## 2026-09-22：src1.3.3A 输入与索引安全层

- 版本：src1.3.3A
- 基线：完整复制 src1.3.2；src1.3.2 保持不变。
- 更改内容：
  - 新增安全整数解析和有界容量策略，限制对象/位置/输入尺寸；负 ID、
    溢出、超大稀疏 ID、错字段数和非法 cross-reference 仅隔离当前事实。
  - 重写 IT form 的安全提取与有界建树，增加 malformed form 的 marker 恢复；
    NL parser 全面增加空 token/空栈/空树/空节点防护。
  - 增加 Instruction Schema，校验 task/info/cons 的参数数量、`X/Y` 绑定、
    对象身份和实际类型；支持 `VALID/REPAIRED/INFERRED/REJECTED`，不因缺失
    可从实际对象推断的类型而机械拒绝。
  - `ObjectPtrCast` 改为标准异常；业务关键路径补齐 `objects[id]`、位置数组、
    对象×对象及对象×位置矩阵的行列检查。
  - 切断 `objects[0]` 的 RDFW 自持有环和 NL 语法树的父子 `shared_ptr` 环；
    约束过滤的固定 100 项临时数组改为有界动态容器。
  - 保持 Candidate 排序、选择和执行策略不变；安全容量检查对预分配范围使用
    常数时间快速路径，避免影响 3B 时间预算。
- 验证结果：Linux 真实 SDK 完整编译/链接通过；原 3A/3B 基础设施测试通过；
  新增负向单测全部通过，ASan/UBSan 在启用 leak detection 时无越界、
  UB 或泄漏报告。`realcompetiton_2024`
  Stage 2 IT 重点 14 题全部正常结束、0 平台硬超时；官方分合计 10012，
  高于 src1.3.2 同集的 9920。13 题受平台随机回答影响的首轮波动复跑为
  496 分，与 src1.3.2 基线一致。详细协议见
  `test-results/realcompetition-src133A-input-safety-20260922/report.md`。

## 2026-09-22：src1.3.2 Stage 3B 确定性任务组边际收益

- 版本：src1.3.2
- 基线：完整复制 src1.3.1；src1.3.1 保持不变。
- 更改内容：
  - 新增 `CandidatePlan` / `CandidateAction` / `CandidatePlanEvaluator`；复用原
    `SolveTask` planner 做无执行器副作用的完整 dry-run，并在评估后恢复 World State、
    任务/约束状态、证据位和动作成本。
  - 复用 `TerminalChecker` 与 `ScoreEvaluator` 对比执行前后状态，记录 gained/lost
    goals、broken/preserved constraints、action cost，以及
    `marginal_score = score_after - score_before`；第一阶段不引入概率模型。
  - shadow mode 输出所有 candidate 的 utility 与稳定排序，以 `legacy_choice` 标记但不
    改变原 planner 选择；暂未加入 `utility <= 0` 硬退出。
  - DeadlineManager 改为只判断 candidate 的完整 remaining plan，移除固定
    300/1000/按任务类型 estimate；执行中输出 remaining-utility，完整 candidate 内不
    依据 utility 提前停止。
  - multi-goto relocation 同样按完整 candidate 做时间判断；修复 hub 位置号被当作
    `SolveTask_PutOn` 对象 ID 的参数错配，只映射既定位置到代表对象，不改变 hub 搜索。
- 验证结果：Windows MinGW C++11 全量语法检查通过；Linux 使用真实 SDK 头文件与静态库
  完整编译/链接通过；3A/3B 单元测试覆盖 CandidatePlan 动作封装、dry-run 状态恢复、
  gained goal、broken constraint、动作成本、边际分和 remaining-utility，全部通过。
  `realcompetiton_2024` Stage 2 IT 重点题 11、13、16、19、21、22–24、28–30、34–36
  共 14 题全部正常结束、0 次平台硬超时；官方分合计 9920，完成 goals 合计 299。
  19/21/28/29/30 分别完成 15/18/15/15/19 个 goals，22–24、34–36 均达到官方
  1000 分封顶。详细协议见
  `test-results/realcompetition-src132-stage3b-20260922/report.md`。

## 2026-09-21：初始化公开仓库

- 版本：初始版本
- 更改内容：初始化 Git 仓库，新增仓库结构与提交要求说明。
- 代码变更：无；现有代码保持不变。
- 验证结果：目录结构、文件大小和常见敏感信息检查通过。

## 2026-09-21：scr1.3 must-near 修复

- 版本：scr1.3
- 基线：完整复制 scr1.2；scr1.2 保持不变。
- 更改内容：
  - must-near/nextto 关系改为对象 ID × 对象 ID 的对称关系图，支持 `every` 产生的多对象 `X × Y`，不再把 `on` 混入 near 图。
  - must-near 查找表只按对象容量扩展，禁止再用 location 扩容，延续 scr1.2 的 object/location 分离。
  - 新增直接位置证据与 must-near 推导证据的来源区分；直接证据优先，平票、直接证据冲突及 Sense 反证均停止传播。
  - 感知成功可沿 must-near 连通分量传播可信位置；容器仅同步确定在其内部的物体，不改写 `inside/on` 关系。
  - AskLoc 与原子动作完成后刷新 must-near 满足/锁定状态，但动作证据不会触发组内物体“瞬移”。
- 验证结果：使用临时 `cserver/plug.hpp` 接口桩完成 `rdfw.cpp`、`parser.cpp`、`main.cpp` 的 C++11 语法编译；must-near 回归程序覆盖对称关系、`X × Y`、排除 `on`、单源证据传播、冲突证据不覆盖及 Sense 反证优先，全部通过。

## 2026-09-21：src1.3.1 确定性终态检查与时间预算

- 版本：src1.3.1
- 基线：完整复制 scr1.3；scr1.3 保持不变。
- 更改内容：
  - 新增只读 `TerminalChecker`，按真实 World State 与证据位对 goal/constraint 输出 `SATISFIED / UNSATISFIED / UNKNOWN`，并让 Stage 2 零动作检查复用同一判定源。
  - 新增 `ScoreEvaluator`，按 2026 规则统计确定完成目标、确定满足约束、所有已尝试动作成本与确定性基础分；不加入效率分或概率模型。
  - 新增无执行器副作用的 `DeadlineManager`，提供 elapsed、remaining、deadlineReached 与 canFinish；默认预算 4700ms、安全余量 300ms。
  - 在主任务循环、检查阶段、MustChooseOne 与 multi-goto 前接入 stop gate；目标全完成、无候选或时间不足时由主线程正常停止，单个超预算候选会跳过并继续尝试其他候选。
  - 新增 `[3A]` 进度日志，记录完成目标/约束、确定性基础分、累计动作成本、elapsed 与 remaining。
  - Stage 2 初始 hold/plate 不再直接标记为已验证，避免把可能错误的初始描述当作确定终态。
- 验证结果：使用 2026 比赛 SDK 完成 C++11 全量编译和链接；`tests/three_a_tests.cpp` 覆盖三态判定与执行标志解耦、Stage 2 未验证状态、禁止动作约束的 UNKNOWN、动作成本/基础分公式和 canFinish 边界，测试通过。真实平台 Stage 2 IT 冒烟题 01/03/09/31 均正常退出、无超时，得分分别为 361/430/608/578；其中题 01 在 3.548 秒主动停止，日志 snapshot 的 11 个确定完成目标和 107 分动作成本与平台基础分 333 完全一致。
- 36 题回归：使用 `realcompetiton_2024`、IT 模式、5000ms 平台时限，对 src1.3/src1.3.1 各重复 2 轮（共 144 次）。02.xml 因原题结束标签错误排除；其余 35 题官方分总和均值从 20146.5 降到 15527.5，平台硬超时从 22/70 降到 0/70，但完成 goal 总数均值从 543.5 降到 340.0。固定 1000/1750ms 计划估时加 300ms margin 当前过于保守。最终 snapshot 的动作成本 70/70 与平台一致；基础分仅 34/70 完全一致，35 次为保守低估，16 题第 1 轮出现一次高估。完整协议、逐题结果与动作差异保存于 `test-results/realcompetition-src13-vs-src131-20260921/`。
