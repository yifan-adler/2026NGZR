# 约束收益规划：赛题回归（2026-09-24）

使用固定随机种子 `20260924`、同一批题目和 `src1.5/tests/run_constraint_regression.py`，对照仓库原有 `realcompetition-src1.5-all36-seed20260924/results.json`。原记录中 02 格式错误、04 和 05 评分失败，故只比较其余 33 道有效题。Windows 检出的题目文件若仅因 CRLF 与原记录哈希不同，运行器使用 LF 副本；其他内容变化会直接报错。

33 道题的平台状态均为 `ok`。以目标和约束最终计分减动作成本得到的基础分：22 题提高，11 题持平，0 题下降；基础分合计提高 1416。官方原始分在本轮对照中也无下降，不过时间分受运行机器影响，不用它替代基础分判断。完整逐题数据见 [`results.json`](results.json)。保留了第 11、22 题的原始日志，便于核查时间与终局位置问题。

第 11 题的旧收尾逻辑在时间充裕时会移动到另一个聚合点，丢失多个已满足的 `goto` 目标；修改后先投影该移动的目标与约束终态，负收益时保持原位。第 22 题涉及大量共享目标，验证了单候选报告的“多条约束受损”标记不能直接当成硬性禁行条件；最终得分仍需按可执行计划比较。
